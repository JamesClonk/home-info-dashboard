---
applications:
- name: weather_app
  memory: 64M
  disk_quota: 128M
  instances: 2
  # buildpack: https://github.com/cloudfoundry/buildpack-go.git
  # hosts:
  # - www
  # domains: []
  services:
  - weatherdb
  env:
    DEFAULT_CANTON: Bern
    DEFAULT_CITY: Grächwil
  command: weather_app
  path: .
